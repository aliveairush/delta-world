import React from "react";
import './Header.scss';

const Header = () => (
  <header className="page-layout__header header custom-container">
    <div>HEADER</div>
  </header>
);

export default Header;
